#include<stdio.h>
#include<stdlib.h>
int main()
{
    int l,h,i,j;
    scanf("%d",&l);
    scanf("%d",&h);
    for(i=l;i<=h;i++){
        for(j=l;j<=h/2;j++){
            if(i%j==0){
                break;
            }

        }
         printf("%d\t",i);
    }


}
